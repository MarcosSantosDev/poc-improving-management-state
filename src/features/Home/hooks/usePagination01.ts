import { createPaginationStore } from '@/hooks/createPaginationStore';

export const pagination01StoreKey = 'pagination-1';

export const usePagination01 = createPaginationStore(pagination01StoreKey);
